//***************BoSL camera config file*********
//adjust to suit your application
//ording is importaint to correct reading of file
//be sure to not change the ordering of the line
//an incorrect configuration will cause the program to lock up
//
//name this file 'config.txt' and place in the root directory of the sd card
//
//FOLDERS MUST BE PRESENT ON THE SD CARD AND FTP SERVER FOR
//THE PROGRAM TO FUNCTION. FAILURE TO DO SO WILL RESULT IN LOST IMAGES.
//***********************************************

//***********************************************
//feel free to add notes about this config here:
//
//
//
//
//
//
//
//
//**********************************************

image_config_t
    uint32_t auto_range_time            = 10000
    enum image_resolution resolution    = VGA                       // VGA, QVGA
    enum image_format format            = JPG                       // BMP, JPG
    enum flash_t flash                  = LED_EXTERNAL_FLASH        // NO_FLASH, LED_INBUILT_FLASH, LED_EXTERNAL_FLASH 
    uint32_t use_flash                  = 0                         // 0: flash off, 1: flash on
    aec_t aec                           = AEC_OFF                   // AEC_OFF, AEC_ON
    uint16_t exposure                   = 100                       // 0 to 65537
    agc_t agc                           = AGC_OFF                   // AGC_OFF, AGC_ON
    gain_t gain                                                     // gain struct represented in floating pointesq form
        uint8_t mantissa                = 11                        // 0 to 15
        uint8_t exponent                = 5                         // 0 to 6
ftp_config_t
    char* apn                           = "simbase"
    char* mccmnc                        = "50501"
    char* domain                        = "ftp.bosl.com.au"
    char* username                      = "images2023@bosl.com.au"
    enum cypher_t cypher                = SUFFIX                    // NONE, CAESAR, SUFFIX
    char* password                      = "bosl" 
    char* image_path                    = "/jpg/images/img"
    char* status_path                   = "/jpg/status"
sd_config_t 
    char* image_path                    = "/dne/images/img"
    char* status_path                   = "/dne/status"
    uint32_t logging_level              = 0                         // DBG, INF, WRN, ERR, OFF
trigger_config_t 
    enum trigger_t trigger              = TIME_TRIGGER              // TIME_TRIGGER, UART_TRIGGER
    uint32_t logging_interval           = 360000                    // ms (90000 is near the maximum logging frequency possible, longer than a day will also not work (causes watchdog timer to starve))
    uint32_t logging_decimation_ftp     = 1                         // 1 in every x photos captured to sd will be uploaded
    uint8_t dark_noup                   = 30                        // [0,255], mean R+B+G value below which to not upload. 255 to disable. (< 20 cuts most nighttime images, > 40 keeps most daytime)
    uint32_t network_interval           = 12000                     // ms polling interval for network trigger
    char* network_domain                = "bosl.com.au"             // domain for network trigger 
    char* network_site                  = "/IoT/AquaforBeech/scripts/ReadMe_v2.php?SiteName=SC.csv&Key=Temp" // site for network trigger 


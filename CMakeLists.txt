# SPDX-License-Identifier: Apache-2.0

cmake_minimum_required(VERSION 3.20.0)
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(native_camera)

set(APP_SOURCE_DIR ${PROJECT_SOURCE_DIR}/src)

set(LEX_YY_C ${CMAKE_BINARY_DIR}/lex.yy.c)
set(Y_TAB_H ${CMAKE_BINARY_DIR}/y.tab.h)
set(Y_TAB_C ${CMAKE_BINARY_DIR}/y.tab.c)
set(CONFIG_L ${APP_SOURCE_DIR}/config.l)
set(CONFIG_Y ${APP_SOURCE_DIR}/config.y)


target_sources(app PRIVATE src/main.c)
target_sources(app PRIVATE src/ov7675.c)
target_sources(app PRIVATE src/sram.c)
target_sources(app PRIVATE src/ftp.c)
target_sources(app PRIVATE src/sd.c)
target_sources(app PRIVATE src/util.c)
target_sources(app PRIVATE src/jpg.c)
target_sources(app PRIVATE src/common.c)
target_sources(app PRIVATE src/watchdog.c)

target_sources(app PRIVATE ${LEX_YY_C})
target_sources(app PRIVATE ${Y_TAB_C})

# unit tests 
target_sources(app PRIVATE tests/test.c)
target_sources(app PRIVATE tests/test.modem.c)
target_sources(app PRIVATE tests/test.main.c)


include_directories(app PUBLIC 
     "${APP_SOURCE_DIR}"
     "${CMAKE_BINARY_DIR}" #for y.tab.h
     )

# if (UNIX)
# 	##for flex on linux
# 	add_custom_command(
# 	    OUTPUT ${LEX_YY_C} # The output generated by the code generator
# 	    COMMAND flex -o${LEX_YY_C} ${CONFIG_L} 
# 	    DEPENDS ${CONFIG_L} # Add dependencies like input files or the code generator executable
# 	    COMMENT "Flex: generating lexer"
# 	)

# 	##for yacc on linux
# 	add_custom_command(
# 	    OUTPUT ${Y_TAB_C} ${Y_TAB_H} # The output generated by the code generator
# 	    COMMAND bison -d -o${Y_TAB_C} ${CONFIG_Y} 
# 	    DEPENDS ${CONFIG_Y} # Add dependencies like input files or the code generator executable
# 	    COMMENT "Bison: generating parser"
# 	)

# elseif(WIN32)
	##for flex on windows via wsl
	add_custom_command(
	    OUTPUT ${LEX_YY_C} # The output generated by the code generator
	    COMMAND wsl flex -o`wslpath -u ${LEX_YY_C}` `wslpath -u ${CONFIG_L}` 
	    DEPENDS ${CONFIG_L} # Add dependencies like input files or the code generator executable
	    COMMENT "Flex: generating lexer"
	)

	##for yacc on windows via wsl
	add_custom_command(
	    OUTPUT ${Y_TAB_C} ${Y_TAB_H} # The output generated by the code generator
	    COMMAND wsl yacc -d `wslpath -u ${CONFIG_Y}` -o`wslpath -u ${Y_TAB_C}` 
	    DEPENDS ${CONFIG_Y} # Add dependencies like input files or the code generator executable
	    COMMENT "Yacc: generating parser"
	)
# endif()

